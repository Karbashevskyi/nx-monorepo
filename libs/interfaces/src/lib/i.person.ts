export interface IPerson {
  contact: {
    socialMedia: {
      linkedin: string;
      github: string;
      instagram: string;
      facebook: string;
      x: string;
    };
    email: string;
  };
}
